import 'package:flutter/material.dart';

class TourGeneratorPage extends StatelessWidget {
  const TourGeneratorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🗺️ Tourgenerator')),
      body: const Center(child: Text('Bald verfügbar')),
    );
  }
}
